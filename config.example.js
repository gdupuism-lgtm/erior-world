// Copia este archivo como config.js y pon tus credenciales de Supabase.
// En Netlify: Site settings → Environment variables → copia a config.js en build,
// o edita config.js directamente (no subas config.js a GitHub — está en .gitignore).

window.ERIOR_CONFIG = {
  SUPABASE_URL: 'https://TU-PROYECTO.supabase.co',
  SUPABASE_ANON_KEY: 'tu-anon-key-aqui',
};
